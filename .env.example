# Backend API base URL (no trailing slash). Used by Next.js and by Vite when VITE_BACKEND_URL is unset.
NEXT_PUBLIC_BACKEND_URL=https://api.autobutik.se
BASE_PATH=https://api.autobutik.se

# Vite dev only: client-side code reads this (Vite exposes only VITE_*). Set to backend URL or leave empty to use proxy.
# VITE_BACKEND_URL=https://api.autobutik.se
